# Production release evidence - 2026-06-20 (roadmap P1 iteration)

## Artifact identity

- Code commit: `25e061d1ab56ad40c61066182344c9fc1c68500c`
- Signed tag: `release-2026-06-20-roadmap-p1`
- Signing key fingerprint:
  `SHA256:U9coVPxh7/2ijZ4DEPaHKnmi+oP329jxPC+hm/uzTdo`
- Production image ID:
  `sha256:e6a1f3764259b170e89f912bb6d2b0739e975b682a786fc9dec4d53a6146ec6b`
- Rollback image ID (prior release `e3af346`):
  `sha256:b436498840af3292294d0ced66a8af733e71b20c303761bea6cd8658080737f7`
- Deployment completed: `2026-06-20T10:28Z`
- Schema migrations applied: `001_core.sql`, `002_practice.sql`

## Scope

Roadmap P1 iteration: continuous integration, ordered immutable database
migrations, an expanded test matrix, runtime-configuration extraction, release
smoke tooling, and scheduled backups with a verified restore drill.

## Pre-deployment evidence

- `npm run check`: passed.
- `npm run migrate:check`: fresh database builds all migrations and a second apply
  is idempotent; core tables present.
- `npm test`: 25 passed, 0 failed (up from 15), including migration
  idempotency/baseline/immutability, backup-and-restore round trips, and HTTP
  integration tests for per-identity CSRF, identity isolation, and oversized input.
- Public-tree guard: 0 forbidden files tracked.

## Migration safety

The production database already held the live schema. Applying the migration
baseline created the `schema_migrations` ledger and recorded `001_core.sql` and
`002_practice.sql` at `2026-06-20T10:28:33Z` as an idempotent no-op. The
post-deploy restore drill confirmed all data survived: 542 identities, 26
attempts, 338 practice responses, 25 audit-log rows.

## Deployment and rollback

- A consistent pre-deploy backup (`pre-20260620T102820Z.sqlite`, `PRAGMA
  quick_check = ok`, mode `0600`, 2.19 MB) was taken from the running container
  before any change.
- The prior image was tagged `adaptive-test-app:prev-r2` as the rollback point.
- The new image was built, the container recreated, and the deploy was gated on
  container health followed by the smoke test. Any health or smoke failure would
  have retagged the previous image and recreated the container automatically.
- The hardening overlay (`docker-compose.override.yml`: read-only root, `/tmp`
  tmpfs, `no-new-privileges`, all capabilities dropped) was preserved unchanged.

## Post-deployment evidence

- Running image: `sha256:e6a1f376…`, container health `healthy`.
- On-box smoke test (new container): `failed: 0` across user home/practice `200`,
  admin path on the user host `404`, user write without CSRF `403`, admin app on
  the admin host `200`, user API on the admin host `404`, unauthenticated admin
  API `loggedIn: false`, and unknown Host `421`.
- External HTTPS via Cloudflare: `https://test.feierbuqiu.uk/` `200`,
  `/practice` `200`, `/admin` `404`.
- Migration ledger on production: `001_core.sql`, `002_practice.sql` applied.

## Scheduled backups (OPS-004)

- A `systemd` timer `adaptive-backup.timer` runs daily at `18:00 UTC`
  (`Persistent=true`), invoking a consistent SQLite backup followed by an
  automated `restore-check.mjs` integrity verification, with bounded retention.
- The drill ran once on deploy day: `auto-20260620T103030Z.sqlite`, integrity
  `ok`, ledger `[001_core.sql, 002_practice.sql]` present in the restored copy.

## Explicitly unchanged

- No API key, token, password, TOTP secret, or SSH key was rotated.
- OneDrive organization-account settings and synchronization were not changed.
- No Cloudflare Challenge, Turnstile, Access interstitial, or other human
  verification was enabled.
