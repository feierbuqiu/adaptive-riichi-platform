# Production release evidence - 2026-06-20

## Artifact identity

- Commit: `e3af346e67576e9864edf47101d67d51719fc2ed`
- Signed tag: `release-2026-06-20-e3af346`
- GitHub commit verification: `verified=true`, `reason=valid`
- GitHub tag verification: `verified=true`, `reason=valid`
- Signing key fingerprint:
  `SHA256:U9coVPxh7/2ijZ4DEPaHKnmi+oP329jxPC+hm/uzTdo`
- Production image ID:
  `sha256:b436498840af3292294d0ced66a8af733e71b20c303761bea6cd8658080737f7`
- Deployment completed: `2026-06-20T09:04:57Z`
- Schema migration: none

## Pre-deployment evidence

- `npm.cmd run check`: passed.
- `npm.cmd test`: 15 passed, 0 failed.
- Isolated EC2 image smoke test: health, Host isolation, administrator boundary,
  read-only root, and cleanup all passed.
- SQLite backup: created with mode `0600`; `PRAGMA quick_check` returned `ok`.
- Previous application source and image: retained as rollback points.

## Post-deployment evidence

- Public home and practice routes: `200`.
- Public `/admin` and `/health/ready`: `404`.
- Administrator `/admin`: `200`; unauthenticated `/api/admin/me` returned
  `{ "loggedIn": false }`.
- User API on the administrator hostname: `404`.
- Unexpected Host at the origin: `421`.
- Cloudflare challenge marker/header: absent on every tested public and
  administrator route.
- Application readiness: healthy; log scan found no fatal, exception, error, or
  failed event.
- Runtime boundary: read-only root, `/tmp` tmpfs, all capabilities dropped,
  `no-new-privileges`, and PID limit `256`.
- Deployed source security files matched the signed release archive.

## Rollback command

From the production deployment directory:

```sh
docker tag adaptive-test-app:rollback-e3af346e6757 adaptive-test-app:latest
docker compose up -d --no-deps --no-build --force-recreate app
```

The pre-release database and source archives must be retained until the next
verified backup/restore drill supersedes this rollback point.

## Explicitly unchanged

- No API key, token, password, TOTP secret, or SSH key was rotated.
- OneDrive organization-account settings and synchronization were not changed.
- No Cloudflare Challenge, Turnstile, Access interstitial, or other human
  verification was enabled.
